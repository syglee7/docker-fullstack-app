language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t zena/react-test-app -f ./frontend/Dockerfile.dev ./frontend

script:
  - docker run -e CI=true zena/react-test-app npm run test

after_success:
  - docker build -t zena/docker-frontend ./frontend
  - docker build -t zena/docker-backend ./backend
  - docker build -t zena/docker-nginx ./nginx

  - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_ID" --password-stdin

  - docker push zena/docker-frontend
  - docker push zena/docker-backend
  - docker push zena/docker-nginx